# Commits that only reformat, which git blame skips: GitHub reads this file
# itself, and locally `git config blame.ignoreRevsFile .git-blame-ignore-revs`
# turns it on (README "Code style").

# style(athina): format all Swift to Google's Swift style
1f4c77892bfffc154599a136a59f412eeb34c951
