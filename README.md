# Evidence for the session-memory HIG pass

Images and notes for the pull request from `fm/mentor-hig-session-memory-surfaces`.
`after/` is `Mentor --snapshot` on that branch; `before/` is the same snapshot states
rendered on `main` before the change (88aefc8 plus the new snapshot states only);
`screen/` is on-screen captures in replay. `audit.md` and `pr-notes.md` are the
audit and notes for the description. Nothing here is merged into `main`.
