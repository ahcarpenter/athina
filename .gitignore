.build/
build/
*.xcodeproj
*.xcworkspace
.swiftpm/
.DS_Store
.lavish/
# Recorded model calls hold whole requests, screen text and screenshots included.
# The app records outside the repository; this keeps `make record RECORD_DIR=recordings` out of git.
recordings/
# The package's dependencies are pinned exactly in Package.swift; a committed
# Package.resolved would have every build fetch the UI smoke test's too (see the traits there).
/Package.resolved
